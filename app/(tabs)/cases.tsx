import { View, Text } from 'react-native';
import React from 'react';

const cases = () => {
    return (
        <View>
            <Text>cases</Text>
        </View>
    );
};

export default cases;